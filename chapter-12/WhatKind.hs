module WhatKind where



-- 1

-- For the signature of `a -> a` the type of `a` is `*`.



-- 2

-- For the signature of `a -> f a` the type of `a` is `*` and the type of `f` is `* -> *` because `f` is being applied (to `a`).
